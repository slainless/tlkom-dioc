<?php
	global $db_name;

	global $my_host;
	global $my_user;
	global $my_pass;

	global $p_conn;
	global $p_defn;

	global $p_dash;

	global $p_main;
	global $p_incl;
	global $p_logn;

	global $r_conn;
	global $r_defn;

	global $r_dash;

	global $r_main;
	global $r_incl;
	global $r_logn;

	global $message_register;
    global $alert_register;

